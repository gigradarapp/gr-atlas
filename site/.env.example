# Copy this file to .env.local and add your own server-side API key.
# Never use a NEXT_PUBLIC_ prefix for this value.
OPENAI_API_KEY=

# Optional comma-separated browser origins allowed to call /api/atlas/chat.
# The request host is always allowed automatically.
ATLAS_ALLOWED_ORIGINS=
